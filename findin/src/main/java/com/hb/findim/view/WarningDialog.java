package com.hb.findim.view;import android.app.ProgressDialog;import android.content.Context;import android.view.View;import android.view.Window;import android.widget.Button;import android.widget.TextView;import com.hb.findim.R;public class WarningDialog {    private ProgressDialog dialog;    public Context context;    private Button btnOk;    private TextView fi_warn_name;    public WarningDialog(Context context) {        this.context = context;        dialog = new ProgressDialog(context, R.style.CustomProgressDialog);        dialog.requestWindowFeature(Window.FEATURE_NO_TITLE);        dialog.setProgressStyle(ProgressDialog.STYLE_HORIZONTAL);// 设置进度条的形式为圆形转动的进度条        dialog.show();//        dialog.setContentView(R.layout.fi_progress_warning);// 设置布局        btnOk = (Button) dialog.findViewById(R.id.fi_btn_ok);        fi_warn_name=(TextView)dialog.findViewById(R.id.fi_warn_name);        dialog.setCancelable(true);        dialog.setCanceledOnTouchOutside(false);    }    public void setName(String name) {        fi_warn_name.setText(name);    }    public void onOK(final View.OnClickListener listener) {        btnOk.setOnClickListener(listener);    }    public void closeDialog() {        if (dialog.isShowing()) {            dialog.dismiss();        }    }}